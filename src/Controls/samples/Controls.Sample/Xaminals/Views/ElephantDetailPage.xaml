<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.Controls.Sample.Xaminals.Views.ElephantDetailPage"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Maui.Controls.Sample.Xaminals.ViewModels"
             mc:Ignorable="d"
             Title="Elephant Details">
  <d:ContentPage.BindingContext>
    <vm:ElephantDetailViewModel />
  </d:ContentPage.BindingContext>
  <ScrollView>
        <StackLayout Margin="20">
            <Label Text="{Binding Elephant.Name}"
                   HorizontalOptions="Center"
                   Style="{DynamicResource TitleStyle}" />
            <Label Text="{Binding Elephant.Location}"
                   FontAttributes="Italic"
                   HorizontalOptions="Center" />
            <Image Source="{Binding Elephant.ImageUrl}"
                   HeightRequest="200"
                   WidthRequest="200"
                   HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Elephant.Details}"
                   Style="{DynamicResource BodyStyle}" />
        </StackLayout>
    </ScrollView>
</ContentPage>
