<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Maui.Controls.Sample.Xaminals.Views.BearDetailPage"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Maui.Controls.Sample.Xaminals.ViewModels"
             mc:Ignorable="d"
             Title="Bear Details">
  <d:ContentPage.BindingContext>
    <vm:BearDetailViewModel />
  </d:ContentPage.BindingContext>
  <ScrollView>
        <StackLayout Margin="20">
            <Label Text="{Binding Bear.Name}"
                   HorizontalOptions="Center"
                   Style="{DynamicResource TitleStyle}" />
            <Label Text="{Binding Bear.Location}"
                   FontAttributes="Italic"
                   HorizontalOptions="Center" />
            <Image Source="{Binding Bear.ImageUrl}"
                   HeightRequest="200"
                   WidthRequest="200"
                   HorizontalOptions="CenterAndExpand" />
            <Label Text="{Binding Bear.Details}"
                   Style="{DynamicResource BodyStyle}" />
        </StackLayout>
    </ScrollView>
</ContentPage>
